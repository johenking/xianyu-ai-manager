[tool.ruff]
target-version = "py312"
line-length = 120
extend-exclude = [
  "data",
  "static",
  "frontend/node_modules",
]

[tool.ruff.lint]
# Start with correctness checks; formatting cleanup remains intentionally separate.
select = ["E9", "F63", "F7", "F82"]
