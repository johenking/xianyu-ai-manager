# 外部门禁

- 2026-08-29 15:5x 代理多租户任务 0 基线偏差（非阻塞，已调整口径继续）：全量 `pytest tests/` = 12 failed + 1101 passed（任务书基线 1103 全绿录于 15:20）。12 个失败全部位于并行登录会话的在制品地界（test_xianyu_official_refresh_integration ×10、test_client_browser_login ×1、test_delivery_center_backend 迁移断言 ×1——对应其未提交的 proxy 改动），与多租户六任务地界零交集，不代修不触碰。本任务完成口径调整为：并行地界失败数 ≤12（快照 /tmp/tenant-task0-baseline.log）、其余测试全绿、新增测试全绿。
- 2026-08-29 拼单修复已上云（pingroup-fix-20260829-144558），但三项外部证据待真实事件：①下一笔真实拼团新单的「秒级发链接→买家点确认→自动免拼+真发货」墙钟金丝雀（用户重开两人小刀入口后才会产生）；②存量收敛完成度——已发过链接的等买家点击、过期账号上的等用户扫码重登、漂移单看对账重发器轮次；③退款单（尾号 005037）用户人工拍板（同意退款或撤销后补发货），系统按 refunding 正确拒发不代办。
- 2026-08-29 AI 图片降级已上云（ai-image-fallback-20260829-152854），观察项待真实买家图片消息：①「AI回复生成失败 … ValueError」应归零（历史 ~20 次/天）；②新 warning `入站图片处理失败，降级为无图回复: reason=…` 的 reason 分布将首次揭示实际失败原因（若集中在 CDN 白名单，需另评估是否补域名——那是新决策不自动执行）；③纯图片消息买家应收到固定引导话术。
- 2026-08-29 发货提速已上云，但现网墙钟金丝雀仍待下一笔真实新单；pytest 与 `/health/ready` 不能代替付款→发货耗时。存量卡单（活跃号遗留、历史号、代点未打开链接）按用户拍板不动（拼团存量除外，见上条）。
- Delivery Center 的候选和生产门禁已完成，但真实供应方调用、真实商品绑定及低风险真实订单仍需逐项金丝雀；隔离候选、合成响应和 UI 验收不替代外部结果。
- 买家评价卖家已有待评价订单发现路径，仍缺真实“写入评价 + 回读状态”闭环；在单订单成功证据前不进入生产自动化，也不与卖家评价买家任务共用状态。
- 邀请履约已闭合付款事件、确认链接 ACK、买家确认、发货消息 ACK、平台发货和本地 `fulfilled`；外部邀请接受、邀请方余额和奖励到账是独立业务证据，不能由消息 ACK 或平台已发货推导。
- 真实订单详情字段和卖家后台曝光/浏览指标仍需账号级真人平台金丝雀；合成响应、DOM 推测、UI 展示和 CI 均不替代平台证据。
- 历史自动好评回补的最后一次只读扫描仍有账号会话过期，覆盖不完整时全局 `--apply` 门禁保持关闭；先恢复对应账号并完成一次无写入重扫。
- 普通账号 `pending_only` 待发货回退和 `skipped_reauth` 已部署并由当前进程加载；逐账号线上观察与一笔低金额真实订单金丝雀仍待完成。
- 扫码免密自动续签（L3）已于 2026-08-25 部署，但首次真实闭环未发生：下次扫码登录须落 `browser_data/user_<unb>` 并置 `has_l3_memory=1`，约 10 小时后 L2 过期时观察免密续签结果（成功=目标；`session_not_renewed` 可重试；`fast_entry_unavailable` 单向 manual 需重扫）；代码测试与迁移演练不替代真机续签证据。CDP 接管冒烟需本机 Chrome 开 `--remote-debugging-port` 且配置 `XIANYU_CHROME_CDP_ENDPOINT` 后另行执行。
- 闲鱼登录、风控、消息、订单和评价接口会漂移。出现会话过期、人工验证或结果不明确时保持失败关闭，完成人工恢复或对账后再继续外部写入。
