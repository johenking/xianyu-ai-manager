# Synthetic MTop fixtures

These files are fabricated contract fixtures. They contain no captured
response, Cookie, token, account identifier, webhook secret, or provider data.
Unknown fields are deliberate and verify that only the adapter allowlist is
returned.
